<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ivanbratoev.festpal" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".frontend.currentfestival.CurrentFestivalConcertsListActivity"
            android:label="@string/title_activity_bands_list" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".frontend.festivallists.DownloadedFestivalsActivity"
            android:label="@string/title_activity_manage_fests" >
        </activity>
    </application>

</manifest>
